name: shopify-cli

on:
  push:
    branches:
      - main
  pull_request: {}

concurrency:
  group: shopify-cli-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build ${{ matrix.package }} with Node ${{ matrix.node }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        node: ['12.22.0', '14.13.1', '16']
        package: ['cli', 'theme', 'app', 'core', 'create-app', 'features']
        os: ['ubuntu-latest', 'macos-latest', 'windows-latest']
    steps:
      - uses: actions/checkout@v1
      - name: Set Node.js
        uses: actions/setup-node@master
        with:
          node-version: ${{ matrix.node }}
          cache: 'yarn'
      - name: Install dependencies
        run: yarn install --ignore-engines
      - name: Build
        working-directory: packages/${{ matrix.package }}
        run: yarn run build
  test:
    name: Test ${{ matrix.package }} with Node ${{ matrix.node }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        node: ['12.22.0', '14.13.1', '16']
        package: ['cli', 'theme', 'app', 'core', 'create-app', 'features']
        os: ['ubuntu-latest', 'macos-latest', 'windows-latest']
    steps:
      - uses: actions/checkout@v1
      - name: Set Node.js
        uses: actions/setup-node@master
        with:
          node-version: ${{ matrix.node }}
          cache: 'yarn'
      - name: Install dependencies
        run: yarn install --ignore-engines
      - name: Test
        working-directory: packages/${{ matrix.package }}
        run: yarn run test
  lint:
    name: Lint ${{ matrix.package }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        package: ['cli', 'theme', 'app', 'core', 'create-app', 'features']
    steps:
      - uses: actions/checkout@v1
      - name: Set Node.js
        uses: actions/setup-node@master
        with:
          node-version: 'lts/*'
          cache: 'yarn'
      - name: Install dependencies
        run: yarn install --ignore-engines
      - name: Lint
        working-directory: packages/${{ matrix.package }}
        run: yarn run lint
  type-check:
    name: Type-check ${{ matrix.package }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        package: ['cli', 'theme', 'app', 'core', 'create-app', 'features']
    steps:
      - uses: actions/checkout@v1
      - name: Set Node.js
        uses: actions/setup-node@master
        with:
          node-version: 'lts/*'
          cache: 'yarn'
      - name: Install dependencies
        run: yarn install --ignore-engines
      - name: Type-check
        working-directory: packages/${{ matrix.package }}
        run: yarn run tsc
