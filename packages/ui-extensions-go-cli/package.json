{
  "name": "@shopify/ui-extensions-go-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "yarn --cwd ../.. run nx run ui-extensions-go-cli:build",
    "test": "yarn test:unit && yarn test:integration",
    "test:unit": "make go-test",
    "test:integration": "make integration-test",
    "version-bump": "lerna version --no-push --include-merged-tags",
    "lint": "gofmt -s .",
    "lint:fix": "gofmt -w -s ."
  },
  "devDependencies": {
    "@shopify/shopify-cli-extensions": "0.2.3"
  }
}

