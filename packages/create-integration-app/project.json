{
    "name": "create-integration-app",
    "root": "packages/create-integration-app",
    "sourceRoot": "packages/create-integration-app/src",
    "projectType": "library",
    "tags": ["scope:create-cli"],
    "targets": {
      "clean": {
        "executor": "nx:run-commands",
        "options": {
          "command": "yarn rimraf dist/",
          "cwd": "packages/create-integration-app"
        }
      },
      "build": {
        "executor": "nx:run-commands",
        "outputs": ["dist"],
        "inputs": ["{projectRoot}/src/**/*"],
        "options": {
          "command": "yarn tsc -b ./tsconfig.build.json",
          "cwd": "packages/create-integration-app"
        }
      },
      "lint": {
        "executor": "nx:run-commands",
        "options": {
          "command": "yarn eslint \"src/**/*.ts\"",
          "cwd": "packages/create-integration-app"
        }
      },
      "lint:fix": {
        "executor": "nx:run-commands",
        "options": {
          "command": "yarn eslint 'src/**/*.ts' --fix",
          "cwd": "packages/create-integration-app"
        }
      },
      "test": {
        "executor": "nx:run-commands",
        "options": {
          "command": "yarn vitest run",
          "cwd": "packages/create-integration-app"
        }
      },
      "test:watch": {
        "executor": "nx:run-commands",
        "dependsOn": ["^build"],
        "options": {
          "command": "yarn vitest watch",
          "cwd": "packages/create-integration-app"
        }
      },
      "type-check": {
        "executor": "nx:run-commands",
        "options": {
          "command": "yarn tsc --noEmit",
          "cwd": "packages/create-integration-app"
        }
      }
    }
}
