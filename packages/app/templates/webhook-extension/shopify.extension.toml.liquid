name = "{{ name }}"
type = "{{ type }}"
handle = "webhooks"
description = "Webhook subscriptions for my app"

[webhooks]
api_version = "2024-01"

[[webhooks.subscriptions]]
topics = ["products/create"]
uri = "https://example.com/hooks"
