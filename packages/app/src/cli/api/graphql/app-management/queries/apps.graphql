query listApps {
  apps {
    id
    key
    activeRelease {
      id
      version {
        name
        appModules {
          uuid
          handle
          config
          specification {
            externalIdentifier
          }
        }
      }
    }
  }
}
