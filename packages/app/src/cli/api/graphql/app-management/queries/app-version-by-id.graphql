query AppVersionById($versionId: ID!) {
  version(id: $versionId) {
    id
    metadata {
      versionTag
    }
    appModules {
      uuid
      handle
      config
      specification {
        identifier
        externalIdentifier
        name
      }
    }
  }
}
