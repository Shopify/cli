query fetchSpecifications {
  specifications {
    name
    identifier
    externalIdentifier
    features
    uidStrategy {
      appModuleLimit
    }
    validationSchema {
      jsonSchema
    }
  }
}
