query fetchSpecifications($organizationId: ID!) {
  specifications(organizationId: $organizationId) {
    name
    identifier
    externalIdentifier
    features
    clientSteps
    uidStrategy {
      appModuleLimit
      isClientProvided
    }
    validationSchema {
      jsonSchema
    }
  }
}
