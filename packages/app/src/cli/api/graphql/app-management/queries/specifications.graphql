query fetchSpecifications($organizationId: ID!) {
  specifications(organizationId: $organizationId) {
    name
    identifier
    externalIdentifier
    features
    uidStrategy {
      appModuleLimit
      isClientProvided
    }
    validationSchema {
      jsonSchema
    }
  }
}
