query GetBulkOperationById($id: ID!) {
  bulkOperation(id: $id) {
    completedAt
    createdAt
    errorCode
    fileSize
    id
    objectCount
    partialDataUrl
    query
    rootObjectCount
    status
    type
    url
  }
}
