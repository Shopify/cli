{
  "name": "@shopify/app",
  "version": "0.5.2",
  "private": true,
  "description": "Utilities for loading, building, and publishing apps.",
  "type": "module",
  "files": [
    "/dist",
    "/npm-shrinkwrap.json"
  ],
  "scripts": {
    "clean": "shx rm -rf dist",
    "build": "true",
    "lint": "prettier -c src/** && eslint src/**/*.ts",
    "lint:fix": "prettier -w src/** && eslint src/**/*.ts --fix",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "tsc": "tsc --noEmit"
  },
  "eslintConfig": {
    "extends": [
      "../../package.json"
    ]
  },
  "dependencies": {},
  "peerDependencies": {
    "@oclif/core": "^1",
    "@oclif/plugin-help": "^5",
    "@shopify/cli-kit": "0.12.0",
    "@shopify/support": "0.5.2"
  },
  "devDependencies": {
    "@shopify/cli-testing": "0.1.0"
  }
}
