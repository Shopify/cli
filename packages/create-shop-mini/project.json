{
  "name": "create-shop-mini",
  "root": "packages/create-shop-mini",
  "sourceRoot": "packages/create-shop-mini/src",
  "projectType": "library",
  "tags": ["scope:create-cli"],
  "targets": {
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm rimraf dist/",
        "cwd": "packages/create-shop-mini"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["dist"],
      "inputs": ["{projectRoot}/src/**/*"],
      "options": {
        "command": "pnpm tsc -b ./tsconfig.build.json",
        "cwd": "packages/create-shop-mini"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm eslint 'src/**/*.ts'",
        "cwd": "packages/create-shop-mini"
      }
    },
    "lint:fix": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm eslint 'src/**/*.ts' --fix",
        "cwd": "packages/create-shop-mini"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "command": "pnpm vitest run",
        "cwd": "packages/create-shop-mini"
      }
    },
    "test-watch": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "command": "pnpm vitest watch",
        "cwd": "packages/create-shop-mini"
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm tsc --noEmit",
        "cwd": "packages/create-shop-mini"
      }
    },
    "refresh-manifests": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm oclif manifest",
        "cwd": "packages/create-app"
      }
    }
  }
}
