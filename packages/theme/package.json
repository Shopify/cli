{
  "name": "@shopify/cli-theme",
  "version": "0.1.0",
  "private": false,
  "description": "Utilities building and publishing themes",
  "keywords": [
    "shopify",
    "shopify-cli",
    "shopify-partners"
  ],
  "license": "MIT",
  "types": "dist/index.d.ts",
  "files": [
    "/dist",
    "/npm-shrinkwrap.json"
  ],
  "publishConfig": {
    "access": "public",
    "@shopify:registry": "https://registry.npmjs.org"
  },
  "scripts": {
    "build": "shx rm -rf dist && tsc",
    "build:watch": "tsc --watch",
    "lint": "prettier -c src/** && eslint src/**",
    "lint:fix": "prettier src/** && eslint src/** --fix",
    "test": "jest",
    "test:watch": "jest --watch",
    "tsc": "tsc --noEmit"
  },
  "eslintConfig": {
    "extends": [
      "../../package.json"
    ]
  },
  "dependencies": {
    "@shopify/cli-core": "0.1.0",
    "@shopify/cli-support": "0.1.0"
  },
  "devDependencies": {},
  "peerDependencies": {},
  "engines": {
    "node": ">=12.0.0"
  }
}
