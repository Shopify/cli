{
  "name": "plugin-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/plugin-app/src",
  "projectType": "library",
  "tags": ["scope:plugin"],
  "targets": {
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm rimraf dist/",
        "cwd": "packages/plugin-app"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist"],
      "inputs": ["{projectRoot}/src/**/*", "{projectRoot}/package.json"],
      "options": {
        "command": "pnpm tsc -b ./tsconfig.build.json",
        "cwd": "packages/plugin-app"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm eslint \"src/**/*.ts\"",
        "cwd": "packages/plugin-app"
      }
    },
    "lint:fix": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm eslint 'src/**/*.ts' --fix",
        "cwd": "packages/plugin-app"
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm tsc --noEmit",
        "cwd": "packages/plugin-app"
      }
    }
  }
}
