{
  "name": "@shopify/features",
  "version": "0.1.1",
  "private": true,
  "scripts": {
    "clean": "true",
    "build": "true",
    "lint": "prettier -c **/*.ts && eslint **/*.ts",
    "lint:fix": "prettier -w **/*.ts && eslint **/*.ts --fix",
    "test": "cucumber-js -p default",
    "tsc": "tsc --noEmit"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^8.0.0-rc.2",
    "@types/cucumber": "^7.0.0",
    "cucumber-junit-formatter": "^0.2.2",
    "cucumber-pretty": "^6.0.1"
  },
  "engines": {
    "node": "^14.13.1 || ^16.0.0 || ^17.0.0"
  },
  "eslintConfig": {
    "extends": [
      "../../package.json"
    ],
    "rules": {
      "no-console": "off"
    }
  }
}
