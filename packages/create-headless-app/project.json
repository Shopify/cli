{
    "name": "create-headless-app",
    "root": "packages/create-headless-app",
    "sourceRoot": "packages/create-headless-app/src",
    "projectType": "library",
    "tags": ["scope:create-cli"],
    "targets": {
      "clean": {
        "executor": "nx:run-commands",
        "options": {
          "command": "yarn rimraf dist/",
          "cwd": "packages/create-headless-app"
        }
      },
      "build": {
        "executor": "nx:run-commands",
        "outputs": ["dist"],
        "inputs": ["{projectRoot}/src/**/*"],
        "options": {
          "command": "yarn tsc -b ./tsconfig.build.json",
          "cwd": "packages/create-headless-app"
        }
      },
      "lint": {
        "executor": "nx:run-commands",
        "options": {
          "command": "yarn eslint \"src/**/*.ts\"",
          "cwd": "packages/create-headless-app"
        }
      },
      "lint:fix": {
        "executor": "nx:run-commands",
        "options": {
          "command": "yarn eslint 'src/**/*.ts' --fix",
          "cwd": "packages/create-headless-app"
        }
      },
      "test": {
        "executor": "nx:run-commands",
        "options": {
          "command": "yarn vitest run",
          "cwd": "packages/create-headless-app"
        }
      },
      "test:watch": {
        "executor": "nx:run-commands",
        "dependsOn": ["^build"],
        "options": {
          "command": "yarn vitest watch",
          "cwd": "packages/create-headless-app"
        }
      },
      "type-check": {
        "executor": "nx:run-commands",
        "options": {
          "command": "yarn tsc --noEmit",
          "cwd": "packages/create-headless-app"
        }
      }
    }
}
