---
'@shopify/cli': minor
'@shopify/cli-kit': patch
---

Add `shopify validate` with `theme`, `graphql`, `functions`, and `components` subcommands for offline validation of Shopify code against bundled schemas and rules.
