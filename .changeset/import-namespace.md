---
'@shopify/app': minor
'@shopify/cli': minor
---

Move `app import-extensions` and `app import-custom-data-definitions` under the new `app import` topic; the old paths still work but are deprecated
