# frozen_string_literal: true

# Dependencies in this files are from integration tests in the
# packages/ui-extensions-go-cli/ package. The long-term plan is
# to port those tests over to Node and eliminate the dependency with Ruby.
source 'https://rubygems.org'

group :test do
  gem 'json'
  gem 'minitest'
  gem 'toml-rb'
  gem 'yaml'
end
