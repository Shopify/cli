{
  "name": "fixture",
  "version": "0.1.0",
  "main": "home/index.js",
  "license": "UNLICENSED",
  "scripts": {
    "shopify": "yarn --cwd .. shopify",
    "build": "yarn --cwd .. shopify app build",
    "dev": "yarn --cwd .. shopify app dev",
    "test": "yarn --cwd .. shopify app test",
    "push": "yarn --cwd .. shopify app push",
    "scaffold": "yarn --cwd .. shopify app scaffold"
  },
  "dependencies": {
    "@shopify/cli": "0.33.1",
    "@shopify/app": "0.33.1",
    "react": "17.0.2"
  },
  "author": "pepicrft"
}
